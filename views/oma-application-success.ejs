<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Application Submitted - OMA</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"/>
</head>
<body class="d-flex flex-column min-vh-100 bg-light">

  <%- include('partials/oma-header', { omaUser: omaUser }) %>

  <div class="d-flex flex-grow-1">
    <%- include('partials/oma-sidebar', { active: 'success', omaUser: omaUser, user: user }) %>

    <div class="flex-grow-1 p-4">
      <div class="card shadow-lg border-success rounded-3">
        <div class="card-header bg-success text-white text-center">
          <h2>Application Submitted</h2>
        </div>
        <div class="card-body text-center">
          <% if (flash.success && flash.success.length) { %>
            <div class="alert alert-success"><%= flash.success[0] %></div>
          <% } %>

          <p class="lead">Your application ID: <strong><%= app.id %></strong></p>
          <p>Status: 
            <span class="badge 
              <%= app.status === 'Approved' ? 'bg-success' : 
                  app.status === 'Rejected' ? 'bg-danger' : 
                  'bg-warning text-dark' %>">
              <%= app.status || 'Pending Review' %>
            </span>
          </p>
          
          <div class="mt-4">
            <a href="/oma/application/<%= app.id %>/pdf" class="btn btn-outline-secondary px-4">Download PDF</a>
          </div>

          <!-- Accordion for reviewing submitted details -->
          <div class="accordion mt-4" id="reviewAccordion">
            <div class="accordion-item">
              <h2 class="accordion-header" id="headingOne">
                <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseReview" aria-expanded="false" aria-controls="collapseReview">
                  Review Submitted Application
                </button>
              </h2>
              <div id="collapseReview" class="accordion-collapse collapse" aria-labelledby="headingOne" data-bs-parent="#reviewAccordion">
                <div class="accordion-body text-start">
                  <dl class="row">
                    <dt class="col-sm-3">First Name</dt>
                    <dd class="col-sm-9"><%= app.first_name %></dd>

                    <dt class="col-sm-3">Middle Name</dt>
                    <dd class="col-sm-9"><%= app.middle_name || '-' %></dd>

                    <dt class="col-sm-3">Surname</dt>
                    <dd class="col-sm-9"><%= app.sur_name %></dd>

                    <dt class="col-sm-3">Date of Birth</dt>
                    <dd class="col-sm-9"><%= app.date_of_birth %></dd>

                    <dt class="col-sm-3">Phone</dt>
                    <dd class="col-sm-9"><%= app.phone %></dd>

                    <dt class="col-sm-3">Email</dt>
                    <dd class="col-sm-9"><%= app.email %></dd>

                    <dt class="col-sm-3">Address</dt>
                    <dd class="col-sm-9"><%= app.address %></dd>

                    <dt class="col-sm-3">Marital Status</dt>
                    <dd class="col-sm-9"><%= app.marital_status || '-' %></dd>

                    <dt class="col-sm-3">Children</dt>
                    <dd class="col-sm-9"><%= app.number_of_children || 0 %></dd>

                    <dt class="col-sm-3">Father Alive</dt>
                    <dd class="col-sm-9"><%= app.father_alive ? 'Yes' : 'No' %></dd>

                    <dt class="col-sm-3">Mother Alive</dt>
                    <dd class="col-sm-9"><%= app.mother_alive ? 'Yes' : 'No' %></dd>
                  </dl>
                </div>
              </div>
            </div>
          </div>
          <!-- End accordion -->

        </div>
        <div class="card-footer text-center text-muted">
          Thank you for applying to join the Family Fund. You’ll be notified once the chairman reviews your application.
        </div>


  <div class="content">
    <h2>📂 My Applications</h2>

    <% if (typeof apps !== 'undefined' && apps.length > 0) { %>
        <table class="table">
        <thead>
          <tr>
            <th>ID</th>
            <th>Full Name</th>
            <th>Date of Birth</th>
            <th>Status</th>
            <th>Submitted At</th>
            <th>Action</th>
          </tr>
        </thead>
        <tbody>
          <% apps.forEach(ap => { %>
            <tr>
              <td><%= ap.id %></td>
              <td><%= ap.first_name %> <%= ap.middle_name || '' %> <%= ap.sur_name %></td>
              <td><%= ap.date_of_birth.toISOString().slice(0,10) %></td>
              <td><%= ap.status %></td>
              <td><%= ap.created_at.toISOString().slice(0,19).replace('T',' ') %></td>
              <td>
                <a href="/oma/application/<%= ap.id %>/success" class="btn btn-sm btn-primary">
                  Review
                </a>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } else { %>
      <p>You have not submitted any applications yet.</p>
      <a href="/oma/apply" class="btn btn-success">Apply Now</a>
    <% } %>
  </div>

      </div>
    </div>
  </div>

  <%- include('partials/oma-footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
