

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Resend Verification - FamilyFund</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/header.css">
  <link rel="stylesheet" href="/css/footer.css">
  <link rel="stylesheet" href="/css/sidebar.css">

  <style>
    body {
      background: #f5f7fb;
      font-family: "Poppins", sans-serif;
    }

    .verify-container {
      max-width: 500px;
      margin: 60px auto;
      padding: 40px;
      background: #fff;
      border-radius: 16px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.1);
      text-align: center;
    }

    .verify-container h2 {
      font-size: 1.8rem;
      color: #333;
      margin-bottom: 15px;
    }

    .verify-container p {
      color: #555;
      font-size: 0.95rem;
    }

    .input-group input {
      width: 100%;
      padding: 12px;
      border: 1.5px solid #ccc;
      border-radius: 8px;
      font-size: 1rem;
      margin: 15px 0;
      transition: border 0.3s ease;
    }

    .input-group input:focus {
      border-color: #007bff;
      outline: none;
    }

    .btn-submit {
      width: 100%;
      padding: 12px;
      border: none;
      border-radius: 8px;
      background: #007bff;
      color: #fff;
      font-size: 1rem;
      font-weight: 500;
      cursor: pointer;
      transition: background 0.3s ease;
    }

    .btn-submit:hover {
      background: #0056b3;
    }

    .success {
      color: green;
      font-weight: bold;
      margin-bottom: 10px;
    }

    .error {
      color: red;
      font-weight: bold;
      margin-bottom: 10px;
    }

    .links {
      text-align: center;
      margin-top: 20px;
    }

    .links a {
      color: #007bff;
      text-decoration: none;
      font-weight: 500;
    }

    .links a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <%- include('partials/header') %>

    <main>
      <div class="verify-container">
        <h2>Resend Email Verification</h2>

        <% if (flash.error?.length) { %>
          <p class="error"><%= flash.error[0] %></p>
        <% } %>
        <% if (flash.success?.length) { %>
          <p class="success"><%= flash.success[0] %></p>
        <% } %>

        <p>If you’re an existing user who needs to verify your email, enter your email address below to receive a new verification code.</p>

        <form action="/auth/resend-verification" method="POST">
          <div class="input-group">
            <input type="email" name="email" placeholder="Enter your email" required>
          </div>
          <button type="submit" class="btn-submit">Send Verification Code</button>
        </form>

        <div class="links">
          <a href="/auth">Back to Login</a> |
          <a href="/auth/signup">Create New Account</a>
        </div>
      </div>
    </main>

    <%- include('partials/footer') %>
  </div>
</body>
</html>


