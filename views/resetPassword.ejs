<!DOCTYPE html>
<html>
<head><title>Reset Password</title></head>
<body>
  <h2>Reset Password</h2>

  <% if (flash.error?.length) { %>
    <p style="color:red;"><%= flash.error[0] %></p>
  <% } %>

  <form action="/auth/reset-password/<%= token %>" method="POST">
    <input type="password" name="newPassword" placeholder="New Password" required minlength="8" />
    <input type="password" name="confirmNewPassword" placeholder="Confirm Password" required minlength="8" />
    <button type="submit">Reset Password</button>
  </form>

  <p><a href="/auth">Back to Login</a></p>
</body>
</html>
