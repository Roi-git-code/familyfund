
<% if (user) { %>
<button id="sidebarToggle" class="sidebar-toggle d-md-none">☰</button>

<nav id="sidebar" class="sidebar">
  <h3>Menu</h3>

  <% if (user.role === 'admin') { %>
    <div class="sidebar-group" data-group="admin">
      <div class="sidebar-group-header">
        <span class="icon">⚙️</span>
        <span class="title">System Administration</span>
        <span class="arrow">▼</span>
      </div>
      <div class="sidebar-group-content">
        <a href="/admin">Dashboard</a>
        <a href="/member/form">Register Member</a>
        <a href="/member">Manage Members</a>
      </div>
    </div>
  <% } %>

  <% if (user.role !== 'admin') { %>
    <!-- Dashboard -->
    <div class="sidebar-group" data-group="dashboard">
      <div class="sidebar-group-header">
        <span class="icon">📊</span>
        <span class="title">Dashboard</span>
        <span class="arrow">▼</span>
      </div>
      <div class="sidebar-group-content">
        <a href="/dashboard">Main Dashboard</a>
      </div>
    </div>

    <!-- Requests -->
    <div class="sidebar-group" data-group="requests">
      <div class="sidebar-group-header">
        <span class="icon">📝</span>
        <span class="title">Requests</span>
        <span class="arrow">▼</span>
      </div>
      <div class="sidebar-group-content">
        <a href="/update-requests">My Requests</a>
        <a href="/update-profile">Requesting System</a>
        <% if (['chairman', 'chief_signatory', 'assistant_signatory'].includes(user.role)) { %>
          <a href="/user-requests">Member Requests</a>
          <a href="/fund-requests">Fund Requests</a>
        <% } %>
      </div>
    </div>

    <!-- Contributions -->
    <div class="sidebar-group" data-group="contributions">
      <div class="sidebar-group-header">
        <span class="icon">💵</span>
        <span class="title">Contributions</span>
        <span class="arrow">▼</span>
      </div>
      <div class="sidebar-group-content">
        <a href="/my-contributions">My Contributions</a>
        <a href="/constitution">Constitution</a>
        <% if (user.role === 'chief_signatory') { %>
          <a href="/contributions/form">Add Contribution</a>
          <a href="/contributions">Manage Contributions</a>
        <% } %>
        <% if (['chairman', 'assistant_signatory'].includes(user.role)) { %>
          <a href="/contributions">View Contributions</a>
        <% } %>
      </div>
    </div>

    <!-- Financial -->
    <div class="sidebar-group" data-group="financial">
      <div class="sidebar-group-header">
        <span class="icon">🏦</span>
        <span class="title">Financial</span>
        <span class="arrow">▼</span>
      </div>
      <div class="sidebar-group-content">
        <a href="/financial-statement">Financial Statement</a>
        <% if (['chairman', 'chief_signatory', 'assistant_signatory'].includes(user.role)) { %>
          <a href="/fund-management">Fund Management</a>
        <% } %>
      </div>
    </div>

    <!-- Loan -->
    <div class="sidebar-group" data-group="loan">
      <div class="sidebar-group-header">
        <span class="icon">🤝</span>
        <span class="title">Loan</span>
        <span class="arrow">▼</span>
      </div>
      <div class="sidebar-group-content">
        <a href="/loan-amortization">Loan Amortization</a>
        <a href="/loan-application">Apply for Loan</a>
        <a href="/my-loans">My Loans</a>
        <% if (['chief_signatory', 'assistant_signatory'].includes(user.role)) { %>
          <a href="/loans/repayment">Loan Repayment</a>
        <% } %>
        <% if (['chairman', 'chief_signatory', 'assistant_signatory'].includes(user.role)) { %>
          <a href="/admin/loans">Manage Loans</a>
        <% } %>
      </div>
    </div>

    <!-- Payments -->
    <div class="sidebar-group" data-group="payments">
      <div class="sidebar-group-header">
        <span class="icon">💳</span>
        <span class="title">Payments</span>
        <span class="arrow">▼</span>
      </div>
      <div class="sidebar-group-content">
        <a href="/payments">Make Payment</a>
        <a href="/payments/history">Payment History</a>
      </div>
    </div>

    <% if (user.role === 'chairman') { %>
      <div class="sidebar-group" data-group="member-mgmt">
        <div class="sidebar-group-header">
          <span class="icon">👥</span>
          <span class="title">Member Management</span>
          <span class="arrow">▼</span>
        </div>
        <div class="sidebar-group-content">
          <a href="/oma/admin/list">OMAS</a>   
          <a href="/member/form">Register Member</a>
          <a href="/member">Manage Members</a>
        </div>
      </div>
    <% } %>
  <% } %>

  <% if (['chairman', 'chief_signatory', 'assistant_signatory', 'admin'].includes(user.role)) { %>
    <div class="sidebar-group" data-group="support">
      <div class="sidebar-group-header">
        <span class="icon">📞</span>
        <span class="title">Support</span>
        <span class="arrow">▼</span>
      </div>
      <div class="sidebar-group-content">
        <a href="/admin/support">Support Messages</a>
      </div>
    </div>
  <% } %>

  <div class="sidebar-group" data-group="security">
    <div class="sidebar-group-header">
      <span class="icon">🔒</span>
      <span class="title">Security</span>
      <span class="arrow">▼</span>
    </div>
    <div class="sidebar-group-content">
      <a href="#" id="changePasswordBtn">Manage Password</a>
    </div>
  </div>

<script>
  // Immediate sidebar group toggle (backup)
  (function() {
    function initLocalSidebar() {
      const groups = document.querySelectorAll('.sidebar-group');
      groups.forEach(group => {
        const header = group.querySelector('.sidebar-group-header');
        const content = group.querySelector('.sidebar-group-content');
        if (!header || !content) return;
        // Remove any existing listener to avoid duplicates
        const newHeader = header.cloneNode(true);
        header.parentNode.replaceChild(newHeader, header);
        newHeader.addEventListener('click', function(e) {
          e.stopPropagation();
          const isActive = group.classList.contains('active');
          if (isActive) {
            group.classList.remove('active');
            content.style.display = 'none';
          } else {
            group.classList.add('active');
            content.style.display = 'block';
          }
        });
        // Set initial display
        if (!group.classList.contains('active')) {
          content.style.display = 'none';
        } else {
          content.style.display = 'block';
        }
      });
    }
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initLocalSidebar);
    } else {
      initLocalSidebar();
    }
  })();
</script>

</nav>
<% } %>


